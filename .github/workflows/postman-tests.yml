name: Run Postman Collection

on:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - master

jobs:
  postman-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install Newman
        run: npm install -g newman

      - name: Run Postman Collection
        run: |
          newman run ../../postman/collections/48197099-756eb453-9906-4d16-b202-4ba0f8fa7d1b.json \
            --reporters cli,junit \
            --reporter-junit-export results.xml
